# Optional app settings for Streamlit Community Cloud > Advanced settings > Secrets.
# Visitors enter their own key and model in the UI; no owner key is needed.
INSIGHTPILOT_LLM_PROVIDER = "openrouter"
INSIGHTPILOT_LLM_TIMEOUT = "90"
INSIGHTPILOT_LLM_ATTEMPTS = "2"
INSIGHTPILOT_MAX_TOKENS = "2048"
